#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../aurelia-cli/bin/aurelia.js" "$@"
  ret=$?
else 
  node  "$basedir/../aurelia-cli/bin/aurelia.js" "$@"
  ret=$?
fi
exit $ret
